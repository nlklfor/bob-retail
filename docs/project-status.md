# BOB Retail — Project Status & Continuity Notes

Single source of truth if you're picking this project back up cold (new machine, new Claude session, whatever). Keep this updated as things change — it decays fast otherwise.

## What this is

BOB Retail: a streetwear e-commerce site, built for a client (not the developer's own business). Guest-only — no customer accounts, guest cart/checkout. Stack: Next.js 16 (App Router) + React + TypeScript + Tailwind CSS + Bun + Supabase (Postgres).

No wishlist — removed 2026-08-21 (client decision). If it comes back later, the pattern to follow is the cart's: a Zustand store with `persist` (localStorage), guest-only, matching the no-accounts requirement.

## Recovering after a fresh machine / Windows reset

Your code is safe — everything is pushed to `https://github.com/nlklfor/bob-retail.git`. What's **not** in git (by design — never commit secrets):

1. Clone the repo, `bun install`.
2. Recreate `.env.local` — see `.env.example` in the repo root for the exact variable names (`SUPABASE_URL`, `SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `NOVA_POST_API_KEY`, `NOVA_POST_SENDER_CITY_NAME`). Get the Supabase values from the Supabase dashboard → this project ("bob-retail", ref `aqtxsnacasvlqaagqaoq`) → Project Settings → API — you still own that account, so these are always recoverable there even if lost locally. Get the Nova Poshta key from your account at `my.novaposhta.ua`. `NOVA_POST_SENDER_CITY_NAME` is the Ukrainian (Cyrillic) name of the city orders ship from, e.g. `Київ` — Nova Poshta's API only accepts Cyrillic input, Latin transliterations are rejected. **Never put real values in `.env.example`** — that file is committed to git (deliberately, as a template); only `.env.local` is gitignored.
3. `bun run dev`.
4. Admin panel: `/admin/login`, staff email `myhub245@gmail.com`. If the password's forgotten, reset it via Supabase Dashboard → Authentication → Users (don't need me for that) — deliberately not writing the actual password anywhere in this repo, since a committed file becomes a permanent secret in git history.

## Architecture snapshot

- **Supabase access is server-only**, never exposed to the browser (no `NEXT_PUBLIC_*` keys). `createPublicClient()` (anon key, RLS-respecting) for storefront reads; `createAdminClient()` (service role, bypasses RLS) for admin/checkout writes. Both guarded by the `server-only` package — importing either into a Client Component is a build-time error.
- **Schema**: `categories`, `products` (price + `images[]` + `sku` live here, not on variants), `product_variants` (`size` + `stock_quantity` + `weight_grams` — one row per size, real independent stock per size), `orders` / `order_items` / `payments`, plus `newsletter_subscribers` / `home_feature_images` / `contact_messages`. RLS on everything; `orders`/`order_items`/`payments`/`newsletter_subscribers`/`contact_messages` have **zero** anon policies — guests never touch them directly. Full breakdown in the Database schema table below.
- **Checkout correctness**: `place_order()` is a Postgres function (`security definer`, granted only to `service_role`) that locks each variant row (`FOR UPDATE`), re-validates real price/stock from the database (never trusts client input), decrements stock, and writes the order — all in one transaction. This is the actual fix for the "two customers buy the last pair at once" race condition, not just a description of it.
- **Admin auth**: Supabase Auth, no public signup anywhere — any account that exists was created manually, so "has a valid session" already means "is staff." `requireStaffSession()` (a DAL function) is checked both in the protected layout and independently in every admin page/Server Action, per Next.js's own guidance that layout-only checks aren't reliable across client-side navigation.
- **Cart**: Zustand store with `persist` (localStorage), guest-only, client-side — matches the no-accounts requirement.
- **Routing**: `app/(storefront)/` (Header/Footer layout) vs `app/admin/` (`login/` outside the protected group to avoid a redirect loop, `(protected)/` wraps the actual staff pages). Route groups `()` don't affect URLs; `[param]` folders are real dynamic route segments.
- **Types**: plain hand-written flat interfaces in `lib/types.ts`, not the full Supabase-generated `Database`/`Row`/`Insert`/`Update` generics — deliberate readability trade-off for a small single-developer project. Documented as a reasonable future upgrade, not a permanent ceiling.

## Database schema

All tables live in `public`, RLS enabled everywhere, migrations are incremental and timestamped in `supabase/migrations/`.

| Table                    | Purpose                                                                                                                                                                                                                                      | Anon access                                   |
| ------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------- |
| `categories`             | Catalog categories                                                                                                                                                                                                                           | Read (public)                                 |
| `products`               | Product name/slug/`sku`/description/price/`images[]`/`is_active` — price lives here, not on variants. `sku` auto-generates (`BOB-00001`...) via a DB trigger on insert if left blank, staff-overridable                                      | Read, only where `is_active = true`           |
| `product_variants`       | One row per size (`size`, `stock_quantity`, `weight_grams`, `is_active`) — the only variant dimension is size, colorways are separate products                                                                                               | Read, only active variants of active products |
| `orders`                 | Guest order header: customer contact info, shipping city/branch, status, subtotal/shipping/total                                                                                                                                             | **None** — only reachable via `place_order()` |
| `order_items`            | Line-item snapshot at time of purchase (name/size/price frozen even if the product changes later)                                                                                                                                            | **None**                                      |
| `payments`               | Payment record per order (`provider`, `status`, `amount`, `external_reference`) — `provider` starts `'stub'` from `place_order()`, flips to `'monobank'` once a real invoice is created; `external_reference` holds the Monobank `invoiceId` | **None**                                      |
| `newsletter_subscribers` | Footer email signups (`email`, unique)                                                                                                                                                                                                       | **None** — write-only via Server Action       |
| `home_feature_images`    | 3 fixed homepage banner slots (`position` 1-3, `image_url`, `label`, `product_id`), staff-edited at `/admin/home-content`                                                                                                                    | Read (public — storefront renders them)       |
| `contact_messages`       | Contact page submissions (`name`, `email`, `social_handle`, `message`)                                                                                                                                                                       | **None** — write-only via Server Action       |

`place_order(p_items, p_customer_name, p_customer_phone, p_customer_email, p_shipping_city, p_shipping_branch, p_shipping_cost)` is the only way an order gets created. It's `SECURITY DEFINER`, revoked from `public`, granted only to `service_role` — so even a leaked anon key can't call it directly. Inside one transaction it: locks each `product_variants` row (`FOR UPDATE`), rejects inactive/out-of-stock/nonexistent variants, computes real totals from the database (never from the client), decrements stock, and inserts the order + order_items + a pending payment row.

## Tooling & conventions

- **Package manager**: Bun (`bun install`, `bun run dev`).
- **Linting/formatting**: ESLint (`eslint.config.mjs`, `eslint-config-next`) + Prettier, wired to run automatically pre-commit via `simple-git-hooks` + `lint-staged` (see `package.json`) — staged `.ts`/`.tsx` files get `eslint --fix` + `prettier --write`, staged `.json`/`.css`/`.md` get `prettier --write`.
- **Testing**: `bun test` (Bun's built-in runner). 22 tests covering the Nova Poshta client, `resolveShippingCost`, and the checkout schema — the highest-stakes logic. `test/setup.ts` (via `bunfig.toml`) stubs the `server-only` package for tests, since it otherwise throws outside Next's own bundler condition.
- **Types**: hand-written flat interfaces in `lib/types.ts` rather than Supabase's generated `Database`/`Row`/`Insert`/`Update` generics — see the note under Architecture snapshot.
- **Reduced-motion testing**: `useReducedMotion()` from Motion always reflects the real OS/device setting — it does _not_ respect `MotionConfig`'s `reducedMotion` override (a real Motion limitation, see `motiondivision/motion#1502`). `lib/useReducedMotionAware.ts` wraps it so animation-gating logic only honors the real setting in `NODE_ENV === "production"` — dev always shows animations regardless of your OS setting, so testing doesn't fight your own accessibility preference; a real production build still correctly respects it for real visitors. Use this wrapper (not the raw Motion hook) for any component gating its own logic on reduced-motion.

## What's built and working

- **Storefront**: homepage (full-screen hero, new-arrivals carousel, admin-editable feature banners, category showcase carousel, once-per-session intro video), `/catalog` (category filter via `?category=`, live search by name _or_ SKU via `?q=` plus a debounced header dropdown), `/products/[slug]` (image carousel, SKU display, stock-aware size selection, collapsible trust/description/delivery accordion, "Ще товари" carousel of other products), `/checkout` (combined cart + checkout — no separate `/cart` page; cart items link to their product page and can be removed with an inline Так/Скасувати confirmation, not a native `confirm()`) → `/order/[id]` confirmation, `/faq`, `/contacts` (real form + `contact_messages` table, big email/phone bar), plus static Про нас/Доставка/Умови оплати/Повернення pages.
- **Quick-add**: hovering (or focusing, keyboard-accessible) a product card anywhere it appears — catalog grid, homepage carousel, "Ще товари" — crossfades to the product's second image and reveals a size picker + add-to-cart button, so a purchase doesn't require opening the product page first.
- **Admin panel** (`/admin`): staff login, product list/create/edit (variants, SKU field with auto-generation hint, device image upload to Supabase Storage, slug auto-fill from name), order list/detail with status updates, and homepage feature-banner editor (`/admin/home-content` — image + caption + linked product per slot).
- Full order lifecycle enum: `pending_payment → paid → processing → shipped → completed`, plus `cancelled`/`payment_failed`.
- Design tokens: white/black base + one restrained third color (`--highlight #9184d9`, interactive/link states only — see `docs/design-direction.md`), near-zero corner radii, applied via Tailwind v4's `@theme` in `app/globals.css`. **Fixel (self-hosted) for typography, full Ukrainian Cyrillic support — real, not a placeholder.** Footer is the one deliberate dark exception to the white base.

## What's explicitly stubbed — not real yet, flagged in code comments

- **Social links**: Footer Instagram/TikTok/Telegram icons still point at `href="#"` — no real profile URLs yet.
- **Contact messages have no admin viewer yet**: they save for real to `contact_messages`, but there's no `/admin` page to read them (same as `newsletter_subscribers` — check via the Supabase dashboard for now, or ask for an admin inbox page to be built).

## Nova Poshta integration (2026-08-16)

Real, not stubbed: `lib/nova-poshta/client.ts` wraps the live Nova Poshta v2.0 JSON API (`Address.getCities`, `Address.getWarehouses`, `InternetDocument.getDocumentPrice`) — verified directly against the real API with the developer's personal key before shipping. The checkout page (`app/(storefront)/checkout/page.tsx`) has real debounced city/branch autocomplete backed by Server Actions in `lib/actions/nova-poshta.ts`, and shows a live shipping-cost preview.

- **Shipping cost is real and dynamic** (distance/weight-based via Nova Poshta, not a flat number), using real per-variant weight (`product_variants.weight_grams`, added 2026-08-16) — a fallback constant in `lib/nova-poshta/pricing.ts` only kicks in if a variant is somehow missing (e.g. deleted mid-checkout), not as the normal path.
- **`resolveShippingCost()`** (`lib/nova-poshta/pricing.ts`) is the single source of truth for shipping cost, called both by the live checkout-page preview and — authoritatively, never trusting whatever the client showed — by `placeOrderAction` itself. Same "server always re-derives, never trusts the client" rule the rest of checkout already followed for price/stock.
- **Sender city** comes from `NOVA_POST_SENDER_CITY_NAME` (Cyrillic city name, resolved to a Nova Poshta city Ref at runtime and cached in memory). If that env var isn't set, or the Nova Poshta API call fails for any reason, shipping cost falls back to a flat 80 UAH rather than blocking checkout.
- Nova Poshta's search API only accepts **Cyrillic** input — Latin transliterations (`"Kyiv"`) return a confusing `"FindByString is not specified"` error rather than empty results. Not an issue for real usage (Ukrainian customers type Ukrainian), but worth knowing if testing manually.
- Real waybill creation (as opposed to search + price quoting) still needs the _client's_ Nova Poshta business account — that's a later phase, unrelated to the developer's personal key used here.

## Monobank acquiring integration (2026-09-03)

Real, not stubbed — using the **client's own** Monobank business token (`MONOBANK_API_KEY`), confirmed real (not the developer's personal account). Verified directly against the live API before shipping (real invoice creation, real status check, real public-key fetch — same "verify against the real source" rule as Nova Poshta).

- **Flow**: `placeOrderAction` still calls `place_order()` first (unchanged — order + order_items + a `pending` payments row, stock decremented, all atomic), then creates a real Monobank invoice (`lib/monobank/client.ts`) for the order total and redirects the customer to Monobank's own hosted payment page (`pageUrl`), not straight to `/order/[id]`.
- **Confirmation**: Monobank redirects back to `/order/[id]` after the customer pays (or cancels/fails), but the _actual_ truth comes from Monobank, not the redirect itself. Two paths write it, both going through the same idempotent `applyInvoiceStatus()` (`lib/monobank/apply-status.ts`) so neither can double-process:
  - **Webhook** (`app/api/webhooks/monobank/route.ts`) — the real-time path. Verifies the `X-Sign` header (ECDSA/SHA-256 over the raw body, against the public key from `GET /api/merchant/pubkey`) before trusting anything in the payload.
  - **Fallback check** — `/order/[id]` itself, if it loads and finds the order still `pending_payment`, calls `GET /api/merchant/invoice/status` directly. Covers the webhook simply not having arrived yet by the time the browser redirects back.
- **Order confirmation page is now status-aware**: `paid` → the real success view (and this is the only place the "order received" email now sends, not at checkout time); `pending_payment` → a waiting view that auto-refreshes every few seconds; `payment_failed`/`cancelled` → a retry view, cart intentionally left untouched so the customer doesn't lose it.
- **Stock on a failed/expired/reversed payment** is released back via a new DB function, `release_order_stock()` (mirrors `place_order()`'s own locking pattern) — called automatically from `applyInvoiceStatus()`.
- **Known gap, not yet built**: an invoice the customer simply abandons (closes the tab, never returns to `/order/[id]`, and Monobank's webhook for `expired` doesn't fire or isn't confirmed to) has no sweep — nothing ever calls the status check again for it, so its stock stays reserved indefinitely. A scheduled job re-checking stale `pending_payment` orders is still needed before relying on this for real inventory accuracy under abandonment.
- **`SITE_URL`** (new env var) is the base URL used for both `redirectUrl` and `webHookUrl` — currently `http://localhost:3000`, **must be updated to the real deployed domain** before this can work for real customers; Monobank cannot reach `localhost`. Until then, real invoice creation works (confirmed live), but a real webhook can never be delivered.
- **Payment method logos** (Visa/Mastercard/plata by mono) render next to the payment option on `/checkout`, from `public/images/footer_*` — despite the filename prefix, placement is checkout-page-only per client decision, not the footer.
- **Apple Pay / Google Pay: deliberately not built (client decision, 2026-09-03)**, not just deferred. Native buttons for either need separate business-level setup disproportionate to this project — Apple Developer Program enrollment + Merchant ID + certificates for Apple Pay, a registered Google Pay merchant account for Google Pay — neither of which exists or is planned. Their logo assets were removed from `public/images/` for the same reason (`footer_apple_pay_*`, `footer_google_pay_dark_bg`) — don't re-add without a real integration behind them. Monobank's own hosted payment page may still offer these wallets on supported devices without us building anything; not confirmed either way, and not something to chase.

## Pending Supabase migrations

Migrations are applied incrementally as the client tests locally — this list is only accurate as of this doc's last edit, **re-check `supabase migration list` / the dashboard before trusting it**:

- Confirmed applied (through 2026-09-03): everything up to and including `20260903120000_add_release_order_stock.sql`.
- **Not yet confirmed applied**: `20260825120000_add_product_sku.sql`, `20260826090000_auto_generate_product_sku.sql`, `20260827100000_create_contact_messages.sql`, `20260909120000_create_price_offers.sql`. Until these run, the SKU column/auto-generation, the contact form's real save-to-DB, and the price-offer ("Торг") feature won't work (the pages themselves don't crash — they degrade gracefully, e.g. the offer form shows a real "спробуйте пізніше" error rather than a stack trace).

## What's next / currently blocked

- **Hosting decided, not yet deployed (2026-09-04)**: self-hosted via Docker + Caddy on a Ukrainian VPS (**HostPro NVMe VPS-1**, ₴592/mo — real pricing taken from the client's own screenshot of HostPro's live order page, see `docs/deployment.md`) — client priority is Ukrainian hosting/billing over deploy convenience, so this is deliberately not on Vercel. Because that plan is only 2GB RAM, the image is built off the VPS and shipped over (`docker save` → `scp` → `docker load`) rather than built in place — verified end-to-end locally, including that exact save/load mechanic. Full runbook in `docs/deployment.md`.
- **Monobank real integration is built** (see the dedicated section above) — what's left is entirely the deployment above: `SITE_URL` needs to point at the real domain before Monobank can deliver a webhook, and a scheduled sweep for abandoned (never-returned-to) pending payments is still not built.
- **Product requests ("Замовити товар") — built (2026-09-03)**: a vertical tab on the storefront opens a form (photo + size required, Instagram required for contact, everything else optional) saving to a new `product_requests` table, with a full admin view at `/admin/product-requests`. Real About Us copy and the real Instagram link are also in.
- **Price offers ("Торг") — built (2026-09-09), migration not yet applied**: a "Запропонувати свою ціну" form on every product page, a slider capped at 300 UAH off the listed price (enforced both client-side and by DB check constraints), saving to a new `price_offers` table with a full admin view at `/admin/price-offers`. No in-app auto-checkout at the haggled price — same manual pattern as product requests: staff review the offer in the panel and, if accepted, contact the customer (phone/email/Telegram/Instagram) with a real Monobank payment link at the agreed price, made the same way real orders' payment links already are.
- **Design/animation pass** — no longer deferred, actively in progress and mostly built (palette, typography, homepage sections, product page, contact page, motion — see `docs/design-direction.md` for the full living log). Open items: an admin inbox for contact/newsletter messages, a real TikTok profile URL (Instagram and Telegram are both real now), and whatever the client raises next.

## Business/process context worth remembering

- Built for a client, not the developer's own business. Client owns the eventual domain, Monobank merchant account, business registration. Pattern: build on the developer's own accounts (Supabase, Nova Poshta test key) during development, swap to the client's real credentials at launch — this already happened once cleanly for Supabase and is the template for the rest.
- Fiscal receipts (РРО/ПРРО) — legally required once real payments go live. Client needs to confirm with their accountant which licensed provider to use (e.g. Checkbox.ua). Not yet built; schema is not blocking it (no code changes needed to add it later).
- Catalog is small by design (max ~50-100 products) — colorways are separate product listings, not variants of one product; the only variant dimension is size. This was a deliberate simplification from an earlier, more generic multi-option schema.
- Git: **build first, then commit** (workflow changed 2026-08-16) — make the code changes, report what changed and what's needed from the client (e.g. a migration to apply), wait for local testing/explicit approval, _then_ commit and push. Straight to `main`, solo repo, no PR/branch workflow. Several changes made in one working session still get split into separate logical commits (one per feature), just all committed together once approved rather than one-by-one as each is finished.
- Working mode: normally step-by-step mentor style (the user writes code, gets it reviewed) per the project's own master prompt — currently in "build directly" mode due to deadline pressure, with architecture/security decisions still surfaced rather than decided silently. This is a deadline-driven exception, not a permanent change — worth confirming which mode is active if picking this back up after a long gap.
